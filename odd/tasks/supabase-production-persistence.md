# supabase-production-persistence

## Objective

Replace Vercel-incompatible local JSON persistence with Supabase Postgres, protect the workspace with single-owner Supabase Auth, and enforce client/brand scoping with database RLS.

## Problem and why

Vercel production logs show workspace requests failing when `JsonFileStore` attempts to write under `/var/task`. Supabase has been created for this project, and the owner authorized integrating it with owner authentication and RLS. `OWNER_NAME` is attribution only, not authentication; connecting a server key without an auth boundary would expose public Server Actions and workspace data.

## Authorized scope

- Implement local application changes for Supabase Auth, SSR session handling, owner-only authorization, Postgres migrations/RLS, repository adapters for all seven domain record types, and safe migration of existing local JSON data.
- Target Supabase project ref: `xbduwatnllbwpvcyxkha`.
- The owner has not authorized use of an ambient Supabase/Vercel session, remote schema changes, transferring `.data` records, changing Vercel settings, pushing, or deploying. Stop before any such remote operation and request explicit authorization for the destination, operation, data, and session.
- Preserve existing owner data; importer must never overwrite a remote row silently.

## Constraints and decisions

- Preserve existing application repository ports and domain contracts; infrastructure adapters and the composition root own Supabase I/O.
- Use request-scoped Supabase SSR clients and authenticated identity. Do not put a service-role/secret key in browser code or `NEXT_PUBLIC_*`; do not rely on Proxy/UI visibility alone for authorization.
- RLS must bind each workspace client and its child records to the authenticated owner; verify scope on reads and writes at both adapter and database layers.
- Owner access uses email/password sign-in (no email-delivery/SMTP dependency) and a server-only `SUPABASE_OWNER_ID` allow-list; fail closed when it is missing or does not match the verified Supabase `sub`. No public registration flow is added; public sign-up stays disabled in Supabase. RLS ties the owner UUID to the client root and verifies child rows through that relationship. `OWNER_NAME` remains review attribution, not an auth mechanism.
- Seed/import must be idempotent and safe with concurrent serverless instances. Existing `.data` currently contains 1 client, 1 brand, 2 audiences, 2 briefs, and 1 proposal; there are no review-decision or result-snapshot collection files.
- UI copy remains professional Spanish; identifiers/comments/technical docs remain English.
- TDD: off (source: `odd/tasks/marketing-workspace.md`, no project/session override). Runner: `npm test` (`tsx --test "src/**/*.test.ts"`). Use ordinary focused functional checks.
- RDD: off (global, verified); no review ceremony.
- Route: delegated direct. Mapping trigger: seven ports/adapters, seed, composition root, auth surface span 4+ files; read-only map delegated. Writer trigger: implementation spans multiple non-trivial files; delegate each bounded work unit. Preparation trigger: Next 16/Supabase SSR documentation research delegated before source writes.
- Delivery forecast: approximately 1,000 authored changed lines; strategy `ask-on-risk` (default). Ask once for PR chain strategy before the first work-unit commit; no push or PR is authorized.
- Pre-existing worktree state to preserve: deleted `.env.example`; untracked `.atl/` and `.codegraph/`.

## Tasks

- [x] **SB1** Add single-owner Supabase Auth and enforce authenticated access in server-rendered data reads, Server Actions, and route handlers. Include auth/session tests and login/logout UX. Route: delegated; writer trigger (multi-file auth boundary). Checks: lint, typecheck, focused auth tests, build.
- [x] **SB2** Add versioned Postgres schema and least-privilege grants/RLS policies for clients, brands, audiences, briefs, proposals, review decisions, and result snapshots. Include scope/ownership policy tests. Route: delegated; writer trigger (migration, policy tests, and docs). Checks: SQL/static checks and Supabase local DB tests if available.
- [x] **SB3** Implement Supabase adapters for all seven repository ports with scoped query/write behavior and adapter tests; keep domain/application ports unchanged. Route: delegated; writer trigger. Checks: focused adapter/isolation tests, lint, typecheck.
- [x] **SB4** Make workspace seeding concurrency-safe and add a non-overwriting, idempotent importer for existing `.data` collections with data-preservation tests. Route: delegated; writer trigger. Checks: seed/import tests and dry-run verification without remote credentials.
- [x] **SB5** Wire server-side Supabase clients/adapters through `src/server/container.ts`, document required env names and owner account bootstrap, remove production dependence on local writes, and complete local verification. Route: delegated; writer trigger. Checks: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, `git diff --check`, local login/workspace smoke check where credentials are available.

## Acceptance criteria

- The canonical workspace route loads without writing to the Vercel function filesystem.
- Unauthenticated users cannot read workspace data or invoke mutations; each server entry point authorizes the owner independently of UI or Proxy checks.
- Every persisted record is scoped to its client and brand in repository queries and RLS policies; cross-scope reads/writes are denied.
- All seven existing repository ports continue to work without domain/application changes.
- Local seed/import can be rerun without duplicating or silently overwriting records; existing data is preserved.
- No Supabase secret is exposed to the browser; remote configuration/import/deployment remain pending separate user authorization.

## Progress and evidence

- Confirmed Vercel logs show `ENOENT` creating `/var/task/.data` and earlier `EROFS` writing JSON temp files under `/var/task`.
- Confirmed current project has no auth middleware/proxy or action guard; `OWNER_NAME` is only approver attribution.
- Mapped all seven ports/adapters, `JsonFileStore`, seed initialization, and composition root. Read-only map did not inspect row contents or credentials.
- Official/current guidance gathered before writing: Next 16 auth and Server Actions require authorization at each server entry point; Proxy is only an optimistic routing layer. Supabase SSR uses request-scoped cookie clients; verified claims (`getClaims`) rather than trusting `getSession` for auth checks.
- The user supplied project ref `xbduwatnllbwpvcyxkha` and authorized owner Auth + RLS. No remote Supabase/Vercel changes have been made.
- **SB1 implementation:** added email/password owner login and logout, Supabase SSR cookie client, Next 16 `proxy.ts` session refresh, fail-closed verified-claims owner allow-list, and explicit authorization on workspace reads, Server Actions, and proposal export. No service-role key or public signup flow was added.
- **SB1 checks observed:** `npm run lint` passed; `npm run typecheck` passed; full suite passed 82/82 with `node --import tsx --test 'src/**/*.test.ts'`; `git diff --check` passed. The project-standard `npm test` invocation previously failed before tests due sandbox `listen EPERM`; equivalent runner passed. Production build remains unverified: Turbopack reruns failed on sandbox IPC `EPERM`, and webpack fallback failed with `Cannot read properties of null (reading 'hash')`.
- **Progress:** SB1 code is implemented and locally verified except build; checkbox and task closure remain pending work-unit commit. User's unrelated `.env.example` deletion and `.atl/` / `.codegraph/` remain untouched. Active branch: `feat/supabase-production-persistence`.
- **SB2 implementation:** ran `supabase init --with-vscode-settings=false --with-intellij-settings=false` (no remote link/push) to scaffold `supabase/config.toml`. Added `supabase/migrations/20260925053019_workspace_schema.sql` with all seven record tables (`clients`, `brands`, `audiences`, `campaign_briefs`, `proposals`, `review_decisions`, `result_snapshots`) mirroring the domain interfaces in `src/modules/*/domain` field-for-field: `text` primary keys (domain ids are opaque strings, not guaranteed UUIDs — see `src/shared/infrastructure/uuid-id-generator.ts` and the seeded ids in `src/shared/infrastructure/seed/ventex-seed.ts`), `jsonb` for nested structures (`product_facts`, `pains`/`objections`, `budget_range`, proposal `content`/`generation`, `period`, `metrics`), and `text[]` for flat string arrays (`constraints`, `assets`, `missing_information`, `source_references`). `clients.owner_id uuid not null references auth.users(id)` carries the verified Supabase auth subject (the `SUPABASE_OWNER_ID` allow-list from SB1) — a separate infra-only column from the domain's `owner` display-name field. Every child table below `clients` carries both `client_id` and `brand_id` and a composite foreign key `(client_id, brand_id) references brands(client_id, id)`, so a row can never be attached to a brand belonging to a different client; `audiences`, `campaign_briefs`, and `proposals` additionally expose a `unique (client_id, brand_id, id)` so `campaign_briefs`, `proposals`, `review_decisions`, and `result_snapshots` can composite-FK into their parent (audience/brief/proposal) scoped by the same client+brand. RLS is enabled on all seven tables; `anon` is explicitly revoked (and `public` revoked first) on every table; `authenticated` is granted select/insert/update/delete only. `clients` policies bind directly on `owner_id = (select auth.uid())`; every child table's four policies (select/insert/update/delete, with `using`/`with check` as applicable per Postgres semantics) use an inline `exists` against `brands`/`clients` checking both `brand_id` and `client_id` match and `clients.owner_id = (select auth.uid())` — no `security definer` functions were used (unnecessary here since the exists check runs as the invoking role and is itself subject to the same RLS). Indexes were added on `owner_id`, `client_id`, and every child table's `(client_id, brand_id)` plus its most-queried foreign key (`audience_id`, `proposal_thread_id`, `brief_id`, `proposal_id`) to support both the RLS `exists` checks and the repository ports' scoped list/thread queries (`listByThread`, `listByProposal`, `getByAudience`).
- **SB2 tests:** added `src/shared/infrastructure/supabase/migration-schema.test.ts` — a static, Docker-free test that parses the migration SQL and asserts (41 assertions): all seven tables are defined; RLS is enabled on each; `anon` has no grants anywhere in the file and is explicitly revoked per table; every table has select/insert/update/delete policies referencing `auth.uid()`; insert policies carry `with check` and update policies carry both `using` and `with check`; and the five scoped child tables carry `client_id`+`brand_id` with the composite brand-scoping foreign key. `docker info` was checked first and is unavailable in this sandbox, so no local Supabase DB was started and `supabase db reset` was not run — this is purely static SQL-text verification, not a live-schema test.
- **SB2 checks observed:** `node --import tsx --test 'src/**/*.test.ts'` — 123/123 passed (82 pre-existing + 41 new). `npm test` (project-standard runner) — also passed 123/123 in this run (no sandbox `listen EPERM` this time). `npm run typecheck` — passed, no errors. `npm run lint` — passed, no errors/warnings. `git diff --check` — passed, no whitespace errors. No remote Supabase operations were run (no `db push`, `link`, `--linked`, or remote schema changes); the CLI project was only locally scaffolded and the migration file only exists on disk pending the parent's push.
- **SB2 decision gaps / assumptions:** (1) `result_snapshots.period` was modeled as a single `jsonb` column mirroring the domain's nested `{ from, to }` object rather than two `date`/`text` columns — flagged for SB3/review in case the adapter would prefer flat columns. (2) `review_decisions.feedback`'s "required for changes_requested" invariant is left to the domain/application layer, not duplicated as a DB `check` constraint, consistent with "preserve existing application repository ports and domain contracts." (3) `budget_range` and `generation` are nullable `jsonb` (not `not null default '{}'`) since the domain models them as `T | null`, not empty structures. None of these required an owner decision on their own — they follow directly from the existing domain shapes — but SB3 adapters should read this migration's column list before mapping.

## Next step

All five tasks (SB1–SB5) are implemented and locally verified on `feat/supabase-production-persistence`. What remains is entirely the owner's: the manual bootstrap checklist above (create the Auth user, set `SUPABASE_OWNER_ID`, disable public sign-up, set Site URL, set Vercel env vars, run the importer, deploy) and the delivery decision the owner already made (finish on this branch, send everything to `main` together — see the session update below). This session performed no remote Supabase/Vercel operation, push, or deploy, per the task's Authorized scope.

## Session 2026-09-25 updates

- Supabase CLI logged in by the owner in their own terminal; repo linked to `xbduwatnllbwpvcyxkha`; `supabase/.temp/` git-ignored.
- `npm run build` passed locally (resolves SB1's previously unverified build).
- SB1 closed in commit `bf53f5e`.
- SB2: parent reviewed the migration SQL; `supabase db push --dry-run` listed only `20260925053019_workspace_schema.sql`. The owner applied `supabase db push` themselves and confirmed via dashboard screenshot that all 7 tables exist in `public` without the "Unrestricted" (RLS-off) badge.
- Delivery decision: the owner chose to finish SB3–SB5 on this branch and then send everything to `main` together (replaces the chained-PR question).
- Env vars belong in Vercel / `.env.local` only: `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_OWNER_ID` (+ existing Gemini/owner vars). Owner-side Supabase setup: create owner user (auto-confirm), disable public sign-ups, set Site URL.
- **SB3 implementation:** added `SupabaseClientProvider` (`() => Promise<SupabaseClient>`, `src/shared/infrastructure/supabase/client-provider.ts`) so every adapter takes a request-scoped client factory instead of a module singleton — production wiring (SB5) will pass `createSupabaseServerClient`. Added `errors.ts` (`unwrapList`/`unwrapMaybe`/`unwrapWrite`, throwing `SupabaseRepositoryError` with operation context and the original `PostgrestError` as `cause` — no swallowed errors) and `verified-owner.ts` (`verifiedOwnerId`, re-derives and fails closed against `SUPABASE_OWNER_ID` from the request's own verified claims via `isAllowedOwner`, reused from SB1). Added a generic `ScopedSupabaseRepository<Row, T>` (`scoped-supabase-repository.ts`) mirroring the existing `ScopedRepository` shape (`list`/`getById`/`save`, plus `findOneBy`/`listBy` for derived lookups) — every query explicitly filters by `client_id` and `brand_id` in addition to RLS, and `getById` re-checks scope the same way the JSON adapter does. Implemented all seven adapters: `SupabaseClientRepository` and `SupabaseBrandRepository` directly (client is the scope root; brand is scoped by `clientId` only) in `src/modules/clients/infrastructure/`, and `SupabaseAudienceRepository`, `SupabaseBriefRepository`, `SupabaseProposalRepository`, `SupabaseReviewDecisionRepository`, `SupabaseResultSnapshotRepository` on top of `ScopedSupabaseRepository` in their respective modules' `infrastructure/`. Every camelCase domain field maps explicitly to its snake_case column (jsonb passed through as-is for `productFacts`/`pains`/`objections`/`budgetRange`/`content`/`generation`/`period`/`metrics`, matching the SB2 migration's column list). `SupabaseClientRepository.save` calls `verifiedOwnerId` and stamps `owner_id` on every upsert — a client can never be attributed to anyone but the allow-listed owner even if a caller skips `requireOwner()`. Ports and domain types were not touched. Added `FakeSupabaseClient`/`FakeQueryBuilder` (`fake-supabase-client.ts`, not a `.test.ts` — a shared test helper) simulating the chainable `.from().select().eq().maybeSingle()/.upsert()` surface and `.auth.getClaims()` entirely in memory, no network — used by all adapter tests plus the generic repository's own tests.
- **SB3 tests:** 21 new tests (mapping round-trips including nested jsonb/array fields, derived lookups like `getByAudience`/`listByThread`/`listByProposal`, cross-brand isolation with the exact id reused across scopes, owner-claim mismatch/missing-claims/missing-env fail-closed cases for `verifiedOwnerId`, and a thrown-with-context assertion for a simulated Postgres write failure) plus the pre-existing 130 (82 SB1 + 41 SB2 + 7 scoped-isolation). All against the in-memory fake — no real Supabase project was reached.
- **SB3 checks observed:** `npm test` — 151/151 passed. `npm run typecheck` — passed after loosening `ScopedSupabaseRepository`'s `Row` type parameter from `Record<string, unknown>` to `object` (TypeScript does not consider a named interface without an explicit index signature assignable to a `Record<string, unknown>`-constrained generic parameter, even though the same interface is fine as a concrete type) and widening `FakeSupabaseClient.seed`'s parameter to `object[]`. `npm run lint` — passed, no errors/warnings. `git diff --check` — passed, no whitespace errors. Committed as `feat(persistence): add Supabase repository adapters` (`b0b48b1`), staging only the new adapter/test files — the pre-existing `.env.example` deletion and untracked `.atl/`/`.codegraph/` were left exactly as found.
- **SB3 decision gaps:** none required owner input; the client-provider/error/scope-filtering choices follow directly from the task instructions and the existing `ScopedRepository`/`IdentifiedRepository` patterns.
- **SB4 implementation:** added `insertIfAbsent(item): Promise<boolean>` — inserts only when no row with this id exists yet, never modifies an existing row — to `IdentifiedRepository`/`ScopedRepository` (JSON/in-memory: existence check and write happen inside one `store.mutate` transform, which `JsonFileStore` serializes per file) and to `ScopedSupabaseRepository`/`SupabaseClientRepository`/`SupabaseBrandRepository` (Supabase: `upsert(row, { onConflict: "id", ignoreDuplicates: true }).select("id")`, i.e. `ON CONFLICT DO NOTHING ... RETURNING`, reading back whether a row was actually inserted). Exposed it on all seven concrete repositories (JSON, in-memory, Supabase) so it is available wherever a port is. `ensureVentexSeed` (`src/shared/infrastructure/seed/ventex-seed.ts`) now writes every seeded row through `insertIfAbsent` instead of `save`'s upsert-and-overwrite; the pre-existing `clients.getById` check is kept only as a fast path (skips four calls on the common "already seeded" case) — correctness no longer depends on it, since two racing callers that both pass that check now both fall through to `insertIfAbsent`, which is safe by construction. `VentexSeedRepositories` gained a new `IdempotentSeedRepository<T>` requirement per repo (`insertIfAbsent`), still satisfied by both JSON and Supabase backends without touching the seven core ports. Added the importer: `src/shared/infrastructure/seed/data-importer.ts` (`importWorkspaceData`, pure — takes already-read source data and target repositories, runs the seven collections sequentially in FK dependency order so e.g. a brand insert can never race ahead of its client's, and for each row does one existence check then, outside a dry run, one `insertIfAbsent`), `read-json-collection.ts` (mirrors `JsonFileStore.readAll`'s ENOENT-is-empty behavior, so an absent `review-decisions.json`/`result-snapshots.json` reads as `[]`), `import-cli-args.ts` (pure `--dry-run`/`--write` parsing, defaults to dry run, rejects passing both), and the thin I/O entry point `scripts/import-data.ts` wired to an `npm run import:data` script.
- **SB4 importer authentication:** the script authenticates as the owner via `supabase.auth.signInWithPassword()` against the public `SUPABASE_PUBLISHABLE_KEY`, using two new server-only env vars — `IMPORT_OWNER_EMAIL` / `IMPORT_OWNER_PASSWORD` (never committed; added to `.env.example` in SB5 with placeholder values). This is the identical authentication path the deployed app uses for the owner (email/password sign-in, per SB1), so every import write is bound by the same RLS as a normal app request; no service-role/secret key is used or supported by this script. The script runs outside a Next.js request (no `cookies()`), so it creates its own plain `createClient(url, publishableKey)` instead of reusing the SSR cookie client, signs in once, and reuses that one authenticated client as every adapter's `SupabaseClientProvider`.
- **SB4 tests:** 17 new tests — `IdentifiedRepository`/`ScopedRepository.insertIfAbsent` (insert-once, never-overwrite, and a same-tick concurrent-race case for each), `ScopedSupabaseRepository`/`SupabaseClientRepository`/`SupabaseBrandRepository`/`SupabaseAudienceRepository`/`SupabaseBriefRepository.insertIfAbsent` (never overwrites, including the owner-stamping path), two new `ventex-seed.test.ts` cases (a concurrent double-seed race producing no duplicates, and a rerun after a manual owner edit that must not clobber it), `data-importer.test.ts` (dry run performs zero writes, a real run inserts only missing rows, an existing destination row is skipped and left untouched, and a rerun is idempotent — all against in-memory repositories, no network), and `import-cli-args.test.ts` (flag parsing). All exercised through fakes/in-memory adapters; `scripts/import-data.ts` itself was never executed in this session (it requires real `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`/owner credentials, which were not run against the remote project, per this task's "no remote data transfer is authorized" constraint) — it was only typechecked and linted.
- **SB4 checks observed:** `npm test` — 173/173 passed. `npm run typecheck` — passed (including `scripts/import-data.ts`, which the project's `tsc` config includes via `**/*.ts`). `npm run lint` — passed, no errors/warnings. `git diff --check` — passed. Not yet committed as of this note; see the next step for the commit hash once made.
- **SB4 decision gaps:** none blocked implementation. One note for the owner: the importer's dry run performs read-only Supabase queries (existence checks) rather than being fully offline, because "no network writes" (not "no network calls") is what this task's dry-run requirement asks for — this session never ran it against the real project either way.
- **SB5 implementation:** wired all seven Supabase adapters (from SB3) into `src/server/container.ts` behind a new `buildRepositories()`. Selection logic is factored into a pure, unit-tested `resolveRepositoryBackend` (`src/server/repository-backend.ts`) so it never has to construct a real adapter to be tested: `"supabase"` whenever `SUPABASE_URL`+`SUPABASE_PUBLISHABLE_KEY` are both set (regardless of environment — a developer can point local dev at Supabase too); `"json"` outside a production runtime when Supabase is unconfigured (local-dev-only fallback); `"unconfigured-production"` when Supabase is unconfigured **and** the runtime is production (`VERCEL` set or `NODE_ENV==="production"`) — `container.ts` throws a clear error in that case instead of constructing the JSON adapters, so no production code path can silently write to the local filesystem once this composition root is reached. The Supabase branch passes `createSupabaseServerClient` (the SB1 request-scoped SSR cookie client) directly as every adapter's `SupabaseClientProvider`.
  - **Build-time carve-out (decision, not asked for verbatim in the task):** `next build`'s "Collecting page data" step imports every route module — including this composition root, at module scope — purely for static analysis, with `NODE_ENV=production` set exactly like the deployed runtime, but it never serves a request or writes real data. Without an exception, `npm run build` would be permanently impossible in any environment lacking live Supabase credentials (this sandbox included, since `.env.local` here only has Gemini vars — verified with `rg -o "^[A-Z_]+=" .env.local`, no Supabase entries). `resolveRepositoryBackend` therefore also checks `NEXT_PHASE` (set by Next.js itself, verified at `node_modules/next/dist/build/index.js:1212`): during `phase-production-build` it returns `"json"` instead of `"unconfigured-production"`. The deployed Vercel runtime never has `NEXT_PHASE=phase-production-build` (that value is build-only; the running server is `phase-production-server`), so the fail-closed behavior at actual request time is unaffected — confirmed both by the phase constants in `next/dist/shared/lib/constants.d.ts` and by four dedicated `repository-backend.test.ts` cases (build-phase-with-NODE_ENV=production and build-phase-with-VERCEL=1 both resolve to `"json"`; the same two configurations without the build-phase marker resolve to `"unconfigured-production"`).
  - Recreated `.env.example` (the user's pre-existing deletion is now superseded — the owner's instructions require documenting env names): all pre-existing vars (`GEMINI_API_KEY`, `GEMINI_MODEL`, `OWNER_NAME`, `DATA_DIR`) plus `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_OWNER_ID`, and the importer's `IMPORT_OWNER_EMAIL`/`IMPORT_OWNER_PASSWORD` — placeholders only, nothing committed with a real value.
  - Updated `AGENTS.md`'s persistence section: retitled "Persistence: Supabase in production, JSON for local dev", describing the same selection rule and pointing at `repository-backend.ts`, and noting that seeding/import write through `insertIfAbsent`.
- **SB5 checks observed:** `npm test` — 180/180 passed (173 SB1–SB4 + 7 new `repository-backend.test.ts` cases). `npm run typecheck` — passed. `npm run lint` — passed, no errors/warnings. `git diff --check` — passed. `npm run build` — passed (Turbopack; all 7 routes compiled, `ƒ` dynamic as before) after the build-phase carve-out above; without it, build failed at "Collecting page data" with exactly the intended `unconfigured-production` error, confirming the fail-closed path actually fires. No local login/workspace smoke check against a live Supabase project was performed — no Supabase credentials are configured in this sandbox's `.env.local`, and the task's "no remote Supabase project" constraint (`odd/tasks/supabase-production-persistence.md` Authorized scope) forbids reaching the linked project (`xbduwatnllbwpvcyxkha`) from this session regardless.
- **SB5 decision gaps:** (1) the `NEXT_PHASE` build-phase carve-out above — a reasonable, tested, and documented refinement of the literal `VERCEL`/`NODE_ENV` check the task specified, needed only to keep `npm run build` reproducible without live secrets; flagged here for the owner's awareness rather than asked as a blocking question, since the alternative (an unbuildable repo without secrets) is clearly worse and the deployed-runtime behavior is unchanged. (2) The owner still needs to perform the bootstrap steps below (create the Auth user, set `SUPABASE_OWNER_ID`, disable public sign-up, set Site URL, set Vercel env vars) before any production deploy — this session did not perform any of them, per the task's remote-operation constraints.

## Owner bootstrap checklist (manual, not run by this session)

Before deploying with Supabase as the persistence backend:

1. **Create the owner's Supabase Auth user.** In the Supabase dashboard (project `xbduwatnllbwpvcyxkha`) → Authentication → Users → Add user, with email/password, and enable auto-confirm (no SMTP/email-delivery dependency, per this task's Constraints). Do this once for the single owner identity.
2. **Copy that user's UID into `SUPABASE_OWNER_ID`.** This is the server-only allow-list value `owner-policy.ts`/`owner-auth.ts` check against the verified JWT `sub` claim on every request — authorization fails closed if it is missing or does not match.
3. **Disable public sign-ups** in Supabase Authentication settings, so no one besides the allow-listed owner can ever create an account (there is no public registration flow in the app either, but this closes the same door at the provider level).
4. **Set the Site URL** (Authentication → URL configuration) to the deployed app's URL, so auth redirects/cookies target the right origin.
5. **Set environment variables in Vercel** (Project Settings → Environment Variables): `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_OWNER_ID`, plus the existing `GEMINI_API_KEY`, `GEMINI_MODEL`, `OWNER_NAME`. Do not set `DATA_DIR`-related expectations in production — the JSON store is never used once `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` are set (see `src/server/repository-backend.ts`).
6. **Run the importer dry run**, then the real import, once (1) and (2) above are done and the owner has a local `.env.local` with `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`/`IMPORT_OWNER_EMAIL`/`IMPORT_OWNER_PASSWORD` set:
   - `npm run import:data -- --dry-run` — reports what would be inserted from the local `.data` collections (1 client, 1 brand, 2 audiences, 2 briefs, 1 proposal), writes nothing.
   - Review the report, then `npm run import:data -- --write` to actually import. Safe to rerun — every write is insert-only and never overwrites an existing row (see SB4).
7. **Deploy to Vercel** and confirm the workspace loads and login works with the owner's credentials — this is the point at which the fail-closed check in `resolveRepositoryBackend`/`container.ts` matters: if `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` are missing on Vercel, the app will refuse to start with a clear error instead of silently trying to write to the read-only function filesystem.
